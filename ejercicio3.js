var a=0;
var b=0.21;

var calculariva=0;

a= parseInt(prompt("Digite un numero"));
calculariva - caliva(a,b);

function claiva (nA, nB){
    const result = (nA * nB);
    iva=(nA+result);
    console.log('Precio con iva'+iva +'Precio sin iva'+nA);
}
